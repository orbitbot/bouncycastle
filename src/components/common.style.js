const j2c = require('j2c')

module.exports.wrapper = j2c.inline({
  max_width: '30em',
  margin: '0 auto 1em',
  background : '#fff',
  border_radius : '2px',
  box_shadow: '0 11px 7px 0 rgba(0, 0, 0, 0.19)',
  padding: '0.8em',
})
